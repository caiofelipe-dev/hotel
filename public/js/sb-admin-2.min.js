/* Placeholder sb-admin-2.min.js
   Replace with the real SB Admin 2 JS from the template package
*/
(function(){
  // minimal helper to avoid JS errors when theme scripts are absent
  if(typeof window.sbAdminPlaceholder === 'undefined'){
    window.sbAdminPlaceholder = true;
  }
})();
